#include "main.h"

/**
 * rev_string - This project reverses a string
 * @b: Input string
 * Return: String reverse
 */

void rev_string(char *b)
{
	char rev = b[0];
	int count = 0;
	int c;

	while (b[count] != '\0')
	count++;
	for (c = 0; c < counter; i++)
	{
		count--;
		rev = b[c];
		b[c] = b[count];
		s[count] = rev;
	}
}
